// Este código concatena dos cadenas de texto.

// Función que une dos textos y devuelve esa unión.
let concatenar = (cadena1, cadena2) => cadena1 + cadena2;

// Pruebas de la función.
console.log(concatenar('Hola', 'mundo')); // Holamundo.