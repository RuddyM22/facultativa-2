// Este código toma dos números y devuelve el resultado de la división de un número por el segundo.

// Función que divide dos números.
let dividirNum = (num1, num2) => num1 / num2;

// Probando la función.
console.log(dividirNum(8, 4)); // 2.
console.log(dividirNum(32, 2)); // 16.