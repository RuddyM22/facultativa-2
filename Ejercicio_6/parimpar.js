// Este código toma un número y devuelve 'true' si es divisible entre 2

// Función que evalúa si el parámetro que recibe es par.
let NumeroPar = numero => numero % 2 === 0;

// Se llama la función y se le envía el parámetro.
console.log(NumeroPar(4)); // true.
console.log(NumeroPar(3)); // false.