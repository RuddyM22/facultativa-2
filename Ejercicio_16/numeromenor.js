// Este código devuelve el mínimo de dos números dados.

// Función que retorna el menor de los números recibidos por parámetros.
let numMenor = (num1, num2) => Math.min(num1, num2);

// Prueba de la función
console.log(numMenor(76, 65)); // 65.
console.log(numMenor(973, 546)); // 546.